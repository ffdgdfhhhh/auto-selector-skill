# Auto Selector Skill — 智能 Skill 路由

## 核心逻辑

能甩就甩，甩不了自己干。

## 会话开始时

1. 调用 MCP 工具 `scan_skills` 扫描项目中的所有可用 skill
2. 如果没有 MCP 工具，手动扫描：package.json scripts、项目结构、配置文件、rules 目录
3. 建立 skill 索引（名称 + 描述 + 触发关键词）

## 每次用户发消息时

1. 分析用户意图，提取关键词
2. 调用 MCP 工具 `match_skill` 或手动匹配：
   - "做个计划" → planning/project-management 相关 skill
   - "写测试" → testing 相关 skill
   - "设计页面" → UI/frontend 相关 skill
   - "部署/发布" → deployment 相关 skill
3. **有匹配** → 用原生选择框确认：
   - 问题: "推荐使用 [skill名] — {它能做什么}"
   - 选项: ✅ 使用 / ❌ 不用
4. **多个匹配** → 列出执行计划，问用户要不要全部执行
5. **没有匹配** → 闭嘴，直接干活

## 规则

- 优先使用 MCP 工具进行匹配，没有 MCP 时回退到手动扫描
- 不过度分析，简单匹配就行
- 没匹配到就直接干活，别说"没找到匹配的工具"
- 用户拒绝后不道歉、不解释
- 用户纠正选择后，记住这个偏好
