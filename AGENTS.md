# Auto Selector Skill — 智能路由 v1.1.0

## 核心规则

能甩就甩，甩不了自己干。

## 工作原理

### 1. SessionStart（会话启动时）

- 扫描所有已安装的 plugin 和 skill
- 建立索引列表（名称 + 描述 + 来源）
- 保存到 `~/.claude/auto-selector-index.json`
- 将完整列表注入系统上下文

### 2. UserPromptSubmit（每次用户消息）

- 读取预建的索引列表
- 让 AI 自己判断用户需求匹配哪个 skill
- 匹配到 → 弹窗推荐
- 没匹配 → 直接回答

## 匹配原则

**按语义理解，不按关键词。**

AI 根据 skill 的名称和描述，判断用户意图是否匹配。
- "帮我做个网页" → UI/UX 设计 skill
- "重构这段代码" → 代码简化 skill
- "写测试" → 测试 skill
- "做个计划" → 项目规划 skill

## 用户交互

- 有匹配 → AskUserQuestion 弹窗："推荐使用 [skill名] — {描述}"
- 用户选"✅ 使用" → 执行 skill
- 用户选"❌ 不用" → 跳过，正常回答
- 用户说"不要用 X" → 记住，下次跳过

## 优势

- **自动适应新 skill** — 添加新 skill 后自动纳入索引，无需改代码
- **AI 自主判断** — 不依赖硬编码关键词，语义理解更准确
- **一次扫描，多次使用** — SessionStart 扫描一次，后续直接读索引
