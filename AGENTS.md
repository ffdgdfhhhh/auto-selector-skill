# Auto Selector Skill — 智能路由

## 核心规则

能甩就甩，甩不了自己干。

## 每次用户发消息时

1. 扫描当前项目的 package.json scripts、配置文件、规则文件
2. 分析用户意图是否匹配某个已有的脚本或工具
3. **有匹配** → 使用 AskUser 工具弹窗确认：
   - 问题: "推荐使用 [工具名] — {它能做什么}"
   - 选项:
     - "✅ 使用" — 执行该工具
     - "❌ 不用" — 跳过，直接回答
4. **没有匹配** → 直接执行用户请求

## 匹配示例

- "做个计划" → 项目管理相关脚本
- "写测试" → test 相关 npm script
- "部署" → deploy 相关脚本
- "启动" → dev/start 相关脚本

## 规则

- 简单关键词匹配即可，不过度分析
- 没匹配到就直接干活
- 用户拒绝后不道歉
- 用户纠正后记住偏好
- 如果有 AskUser 或类似工具可用，优先使用原生弹窗
